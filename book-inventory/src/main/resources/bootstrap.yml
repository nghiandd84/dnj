

server:
  port: ${BOOK_INVENTORY_PORT:0}

spring:
  application:
    name: book-inventory
  cloud:
    config:
      name: book-inventory
      discovery:
        service-id: config # Same application.name of config project
        enabled: true
eureka:
  instance:
    hostname: ${DOMAIN_NAME:localhost}
  client:
    enabled: true
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${EUREKA_URL:http://localhost:8761}/eureka/
# Config logger
logging:
  file: 
    name: D:/logs/book-inventory.log
management:
  endpoint:
    health:
      show-details: always
      show-components: always
    logfile:
      external-file: D:/logs/book-inventory.log
    loggers:
      enabled: true
    shutdown:
      enabled: true
  endpoints:
    web:
      exposure:
        include: '*'
  trace:
    http:
      enabled: true