server:
  port: ${EUREKA_PORT:8761}
  
logging:
  file: 
    path: logs
    name: client.log
#  level:
#    root: DEBUG

spring:
  application:
    name: eureka
  profiles:
    active: local#  cloud:#    config:
#      uri: http://localhost:8801  
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false